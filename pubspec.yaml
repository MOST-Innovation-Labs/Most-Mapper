name: most_mapper
description: YAML driven Dart and C# mapping code generator.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  args: ^2.7.0
  recase: ^4.1.0
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.26.0

executables:
  most_mapper:
